<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>🔧 Configuration</title>
  <link rel="stylesheet" href="/style.css" />
  <style>
    body {
      font-family: sans-serif;
      background: #f9f9f9;
      padding: 20px;
    }
    h1 {
      text-align: center;
      margin-bottom: 30px;
    }
    form {
      max-width: 500px;
      margin: auto;
      background: white;
      padding: 25px;
      border-radius: 12px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }
    label {
      display: block;
      margin-top: 15px;
      font-weight: bold;
    }
    input[type="number"],
    select,
    button {
      width: 100%;
      padding: 10px;
      margin-top: 5px;
      border-radius: 6px;
      border: 1px solid #ccc;
      font-size: 1em;
    }
    button {
      background-color: #007bff;
      color: white;
      border: none;
      cursor: pointer;
      margin-top: 20px;
    }
    button:hover {
      background-color: #0056b3;
    }
    .nav-links {
      text-align: center;
      margin-top: 30px;
    }
    .nav-links a {
      margin: 0 10px;
      color: #007bff;
      text-decoration: none;
    }
    .nav-links a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <h1>⚙️ Timetable Configuration</h1>

  <form action="/config" method="POST">
    <label for="workingDays">📅 Working Days</label>
    <select id="workingDays" name="workingDays" required>
      <option value="5" <%= config.workingDays == 5 ? 'selected' : '' %>>Monday–Friday</option>
      <option value="6" <%= config.workingDays == 6 ? 'selected' : '' %>>Monday–Saturday</option>
    </select>

    <label for="periodsPerDay">🕒 Periods Per Day</label>
    <input type="number" id="periodsPerDay" name="periodsPerDay" min="1" required value="<%= config.periodsPerDay %>">

    <button type="submit">💾 Save Configuration</button>
  </form>

  <div class="nav-links">
    <a href="/">🏠 Home</a> |
    <a href="/teachers">👩‍🏫 View Teachers</a> |
    <a href="/generate">📆 Class Timetables</a> |
    <a href="/teacher-timetables">👨‍🏫 Teacher Timetables</a>
  </div>
</body>
</html>
